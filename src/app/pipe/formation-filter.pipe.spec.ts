import { FormationFilterPipe } from './formation-filter.pipe';

describe('FormationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
